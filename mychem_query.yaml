scenarios:
    - flow:
        - post:
            url: "/v1/query"
            json:
                q: "{{ input_id }}"
                scopes: "{{ scopes }}"
                fields: "{{ fields }}"
